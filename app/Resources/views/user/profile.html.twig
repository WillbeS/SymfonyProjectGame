{% extends 'base.html.twig' %}
{# @var platform \AppBundle\Entity\Platform #}
{% set platform = platform_data.currentPlatform %}

{% block header %}
    {% include 'partials/header.html.twig' %}
{% endblock %}

{% block platformCommon %}
    {% include 'partials/sidebar.html.twig' %}
{% endblock %}

{% block body %}
    <h1>Player Profile</h1>
    <div class="col-sm-5">
        <img width="150" height="150" src="{{ asset('images/avatars/' ~ player.avatar) }}">
        <hr />
        <table class="table">
            <tr>
                <th>Name</th>
                <td>{{ player.username }}</td>
            </tr>
            <tr>
                <th>Settlement</th>
                <td>{{ player.currentPlatform.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ player.description }}</td>
            </tr>
        </table>
    </div>
    {% if app.user and app.user.id == player.id %}
        <a class="btn btn-success" href="{{ path('edit_profile', {'id': platform.id}) }}">Edit Profile</a>
    {% endif %}
{% endblock %}