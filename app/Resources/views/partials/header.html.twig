<header>
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">GameName</a>
            </div>
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li {% if currentPage is defined and currentPage == 'platform' %} class="active" {% endif %}><a href="{{ path('homepage') }}">Home</a></li>
                    <li {% if currentPage is defined and currentPage == 'building' %} class="active" {% endif %}><a href="{{ path('manage_buildings', {'id': platform.id}) }}">Buildings</a></li>
                    <li {% if currentPage is defined and currentPage == 'unit' %} class="active" {% endif %}><a href="{{ path('manage_units', {'id': platform.id}) }}">Units</a></li>
                    <li {% if currentPage is defined and currentPage == 'map' %} class="active" {% endif %}><a href="{{ path('map_local', {'id': platform.id}) }}">Map</a></li>
                    <li {% if currentPage is defined and currentPage == 'player' %} class="active" {% endif %}><a href="{{ path('players_all', {'id': platform.id}) }}">Players</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a href="{{ path('user_mailbox', {'id': platform.id}) }}">
                            <span class="glyphicon glyphicon-envelope"></span> Mailbox
                            ({{ app.user.newMessagesCount }})
                        </a>
                    </li>
                    <li><a href="#"><span class="glyphicon glyphicon-user"></span> {{ app.user.username }}</a></li>
                    <li><a href="{{ path('security_logout') }}"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>